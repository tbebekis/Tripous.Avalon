@{
    ViewData["Title"] = "tp DOM";
}

@await Html.PartialAsync("Demos.IndexLink")

<h1>tp DOM</h1>

<p>This demo checks the first DOM helper functions.</p>

<div class="html-code">
    <div class="demo-panel">
        <label for="DemoInput">Value</label>
        <input id="DemoInput" type="text" value="Tripous" />
        <button id="DemoButton" type="button">Run</button>
        <pre id="DemoOutput"></pre>
    </div>
</div>

<script class="js-code">
    tp.Ready(function () {
        var ClickCount = 0;

        tp.On("#DemoButton", "click", function () {
            ClickCount++;
            var Lines = [
                "tp(\"#DemoInput\") found = " + (tp("#DemoInput") !== null),
                "tp.SelectAll(\"button\").length = " + tp.SelectAll("button").length,
                "tp.Value(\"#DemoInput\") = " + tp.Value("#DemoInput"),
                "click count = " + ClickCount,
                "tp.Ready executed = true"
            ];

            tp.Text("#DemoOutput", Lines.join("\n"));
        });
    });
</script>
